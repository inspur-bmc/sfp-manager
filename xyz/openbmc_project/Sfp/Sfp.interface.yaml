description: >
    Implement to provide sfp manager	
methods:
    - name: Reset
      description: >
          Reset the sfp

properties:
    - name: Present
      type: boolean
      description: > 
              sfp is present or not,readonly
    - name: LowPowerMode
      type: boolean
      description: > 
          disable/enalbe sfp low-power-mode,read/write
    - name: Eeprom
      type: array[byte]
      description: > 
          the raw eeprom value,readonly
